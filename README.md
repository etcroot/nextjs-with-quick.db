# nextjs-with-quick.db
A NextJS boilerplate using quick.db as alternative storage

### Installation & Running
* Install:
`npm i`
* Run Dev Mode:
`npm run dev`
* Build Application:
`npm run build`
* Run Production Mode:
`npm start`
